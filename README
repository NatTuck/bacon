
== What is Bacon? ==

Bacon is a language and compiler targeting the OpenCL platform. The goal of the
project is to provide both easier programmability and better performance.


== Installing Bacon ==

First, install the AMD OpenCL SDK and make sure the AMDAPPSDKROOT is properly set
in your ~/.bashrc file.

Next, type "make prereqs" in the base Bacon directory.

Type "make" to build the Bacon shared library.

Finally, add one line to your ~/.bashrc to setup the environment variables bacon
needs to operate.

  . /path/to/bacon/env.sh

== Using Bacon ==

Take a look at the included examples to get started.
